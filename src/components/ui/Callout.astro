---
/**
 * A caveat about the whole section beneath or above it.
 *
 * Distinct from `.caveat`, which qualifies a single fact and is quieter for
 * that reason. This one is used where the page has to say what it does not
 * know before the reader trusts what follows.
 */
---
<div class="callout"><slot /></div>
